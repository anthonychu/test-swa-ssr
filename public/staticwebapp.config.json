{
    "routes": [
        {
          "route": "/",
          "rewrite": "/api/ssr"
        },
        {
          "route": "/index.html",
          "rewrite": "/api/ssr"
        }
    ],
    "navigationFallback": {
        "rewrite": "/api/ssr"
    }
}